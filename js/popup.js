document.addEventListener('DOMContentLoaded', function() {
  const btn = document.getElementById('test');
  btn.addEventListener('click', () => {
    //chrome.runtime.sendMessage({ currency: 'asd' })
  });
});